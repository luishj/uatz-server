services:
  postgres:
    image: postgres:16
    container_name: uatz-postgres
    environment:
      POSTGRES_DB: uatz
      POSTGRES_USER: uatz
      POSTGRES_PASSWORD: uatz
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

